{
  "name": "batchcutout",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npx serve .",
    "dev": "npx serve .",
    "build": "echo Static site ready",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "resend": "^6.16.0",
    "stripe": "^22.3.0"
  }
}
